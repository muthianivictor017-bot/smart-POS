const fs=require('fs');const path=require('path');const {pool}=require('./db');
(async()=>{const client=await pool.connect();try{await client.query(`CREATE TABLE IF NOT EXISTS schema_migrations(name text PRIMARY KEY,applied_at timestamptz NOT NULL DEFAULT now())`);for(const name of fs.readdirSync(path.join(__dirname,'..','migrations')).filter(x=>x.endsWith('.sql')).sort()){const {rowCount}=await client.query('SELECT 1 FROM schema_migrations WHERE name=$1',[name]);if(rowCount)continue;console.log(`Applying ${name}`);await client.query(fs.readFileSync(path.join(__dirname,'..','migrations',name),'utf8'));await client.query('INSERT INTO schema_migrations(name) VALUES($1)',[name])}console.log('Database is up to date')}finally{client.release();await pool.end()}})().catch(e=>{console.error(e);process.exit(1)});
